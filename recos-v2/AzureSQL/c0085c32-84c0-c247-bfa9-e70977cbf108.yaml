description: 'By default, Azure SQL Database premium tier provisions multiple copies
  within the same region. For geo redundancy, databases can be set as Zone Redundant,
  distributing copies across Azure Availability Zones to maintain availability during
  regional outages.

  '
guid: c0085c32-84c0-c247-bfa9-e70977cbf108
labels:
  area: High Availability
  source: azure-resources/Sql/servers/recommendations.yaml
  sourceType: aprl
links: []
queries:
- arg: '// Azure Resource Graph Query

    // Finds non-zone redundant SQL databases and lists them

    Resources

    | where type =~ ''microsoft.sql/servers/databases''

    | where tolower(tostring(properties.zoneRedundant))=~''false''

    |project recommendationId = "c0085c32-84c0-c247-bfa9-e70977cbf108", name, id,
    tags



    '
resourceTypes:
- Microsoft.Sql/servers
service: Azure SQL
severity: 1
text: Enable zone redundancy for Azure SQL Database to achieve high availability and
  resiliency
