Define non-functional requirements such as SLAs, RTO and RPO.	Defina requisitos no funcionales como SLAs, RTO y RPO.
Perform DR tests	Realizar pruebas de DR
Use Traffic Manager or Front Door as a global load balancer for region failover	Usar el Administrador de tráfico o Front Door como equilibrador de carga global para la conmutación por error de la región
Use Availability Zones if supported in your region	Usar zonas de disponibilidad si se admiten en su región
Use the SLA-backed AKS offering	Utilice la oferta de AKS respaldada por SLA
Use Disruption Budgets in your pod specs	Usar presupuestos de interrupción en las especificaciones del pod
If using a private registry, configure region replication to store images in multiple regions	Si utiliza un registro privado, configure la replicación de regiones para almacenar imágenes en varias regiones
Use managed identities instead of Service Principals	Usar identidades administradas en lugar de entidades de servicio
Integrate authentication with AAD (using the managed integration)	Integrar la autenticación con AAD (mediante la integración administrada)
Limit access to admin kubeconfig (get-credentials --admin)	Limitar el acceso a admin kubeconfig (get-credentials --admin)
Integrate authorization with AAD RBAC	Integrar la autorización con AAD RBAC
Use namespaces for restricting RBAC privilege in Kubernetes	Usar espacios de nombres para restringir los privilegios RBAC en Kubernetes
Use Pod identity for workload identities (addon v2 in preview today)	Usar la identidad de Pod para identidades de carga de trabajo (addon v2 en versión preliminar hoy)
Choose the best CNI plugin for your requirements (Azure CNI recommended)	Elija el mejor complemento de CNI para sus requisitos (se recomienda Azure CNI)
If using Azure CNI, size your subnet accordingly	Si usa Azure CNI, ajuste el tamaño de la subred en consecuencia
If using Azure CNI, check the maximum pods/node (default 30)	Si usa Azure CNI, compruebe el máximo de pods/nodo (predeterminado 30)
If using private-IP LoadBalancer services, use a dedicated subnet (not the AKS subnet)	Si utiliza servicios LoadBalancer de IP privada, utilice una subred dedicada (no la subred AKS)
Size the service IP address range accordingly (it is going to limit the cluster scalability)	Dimensione el rango de direcciones IP del servicio en consecuencia (limita la escalabilidad del clúster)
Filter egress traffic with AzFW/NVA if your security requirements mandate it	Filtre el tráfico de salida con AzFW/NVA si sus requisitos de seguridad lo exigen
If using a public API endpoint, restrict the IP addresses that can access it	Si usa un extremo de API público, restrinja las direcciones IP que pueden acceder a él
Use an ingress controller to expose web-based apps	Usar un controlador de entrada para exponer aplicaciones basadas en web
If using AGIC, do not share an AppGW across clusters	Si usa AGIC, no comparta una AppGW entre clústeres
Use Private Endpoints (preferred) or VNet Service Endpoints to access PaaS from the cluster	Usar puntos de conexión privados (preferidos) o extremos de servicio de red virtual para acceder a PaaS desde el clúster
Use private clusters if your requirements mandate it	Usar clústeres privados si sus requisitos lo exigen
Enable a Kubernetes Network Policy option (Calico/Azure)	Habilitar una opción de directiva de red de Kubernetes (Calico/Azure)
Use Network policies to increase intra-cluster security	Usar directivas de red para aumentar la seguridad dentro del clúster
Use a WAF for web workloads (UIs or APIs)	Usar un WAF para cargas de trabajo web (UI o API)
Use DDoS Standard in the AKS VNet	Usar el estándar DDoS en la red virtual de AKS
Do not use AKS Application Routing Add-On	No utilice el complemento aks application routing
Use the standard ALB (as opposed to the basic one)	Utilice el ALB estándar (a diferencia del básico)
If hybrid connectivity is required, use 2xER or ER+VPN for better availability	Si se requiere conectividad híbrida, use 2xER o ER+VPN para una mejor disponibilidad
Consider using a service mesh for advanced microservice communication management	Considere la posibilidad de utilizar una malla de servicios para la administración avanzada de comunicaciones de microservicios
Store your secrets in Azure Key Vault with the CSI Secrets Store driver	Almacene sus secretos en el Almacén de claves de Azure con el controlador del Almacén de secretos de CSI
If user Service Principals for the cluster, refresh credentials periodically (like quarterly)	Si el usuario entidades de servicio para el clúster, actualice las credenciales periódicamente (como trimestralmente)
Use Azure Policy for Kubernetes to ensure cluster compliance	Usar Azure Policy for Kubernetes para garantizar el cumplimiento del clúster
Separate applications from the control plane with user/system nodepools	Separe las aplicaciones del plano de control con grupos de nodos de usuario/sistema
Label your system nodepool to use it with node selectors	Etiquetar el grupo de nodos del sistema para usarlo con selectores de nodos
Use a private registry for your images, such as ACR	Utilice un registro privado para sus imágenes, como ACR
Scan your images for vulnerabilities	Escanee sus imágenes en busca de vulnerabilidades
Use Azure Security Center to detect security posture vulnerabiities	Uso de Azure Security Center para detectar vulnerabilidades de la postura de seguridad
Define app separation requirements (namespace/nodepool/cluster)	Definir los requisitos de separación de aplicaciones (espacio de nombres/nodepool/clúster)
Use an external application such as kubecost to allocate custs to different users	Utilice una aplicación externa como kubecost para asignar custs a diferentes usuarios
Use ephemeral OS disks	Usar discos efímeros del sistema operativo
If not using ephemeral disks, use larger OS disks for the nodes, especially if many pods/node	Si no usa discos efímeros, use discos de sistema operativo más grandes para los nodos, especialmente si hay muchos pods/nodos
Avoid keeping state in the cluster, and store data outside (AzStorage, AzSQL, Cosmos, etc)	Evite mantener el estado en el clúster y almacene datos fuera (AzStorage, AzSQL, Cosmos, etc.)
If using AzFiles Standard, consider AzFiles Premium and/or ANF for performance reasons	Si utiliza AzFiles Standard, considere AzFiles Premium y/o ANF por razones de rendimiento
If using Azure Disks and AZs, consider dedicating nodes per AZ and WaitForFirstConsumer	Si usa Azure Disks and AZs, considere la posibilidad de dedicar nodos por AZ y WaitForFirstConsumer
Check regularly Azure Advisor for recommendations on your cluster	Consulte periódicamente Azure Advisor para obtener recomendaciones sobre el clúster
Develop your YAML manifests with intelligent text editors such as vscode+kubeadvisor	Desarrolle sus manifiestos YAML con editores de texto inteligentes como vscode+kubeadvisor
Monitor your cluster metrics with Container Insights (or other tools like Prometheus)	Supervise las métricas de su clúster con Container Insights (u otras herramientas como Prometheus)
Store and analyze your cluster logs with Container Insights (or other tools like Telegraf/ElasticSearch)	Almacene y analice los registros de su clúster con Container Insights (u otras herramientas como Telegraf/ElasticSearch)
Configure Diagnostic Settins in your clusters to send API logs to Azure Monitor	Configuración de settinas de diagnóstico en los clústeres para enviar registros de API a Azure Monitor
Configure alerts on the most critical metrics (see Container Insights for recommendations)	Configurar alertas en las métricas más críticas (consulte Container Insights para obtener recomendaciones)
Have a regular process to upgrade the cluster periodically (quarterly, for example)	Tener un proceso regular para actualizar el clúster periódicamente (trimestralmente, por ejemplo)
Use kured for Linux node upgrades	Usar kured para actualizaciones de nodos Linux
Consider gitops to deploy applications or cluster configuration to multiple clusters	Considere gitops para implementar aplicaciones o configuración de clúster en varios clústeres
Consider spot node pools for non time-sensitive workloads	Considere los grupos de nodos puntuales para cargas de trabajo no sensibles al tiempo
Consider AKS virtual node for quick bursting	Considere el nodo virtual AKS para ráfagas rápidas
Make sure no changes are performed by operators in the node RG (aka 'infra RG')	Asegúrese de que los operadores del nodo RG no realicen ningún cambio (también conocido como 'infra RG')
Use custom Node RG (aka 'Infra RG') name	Usar el nombre personalizado de Node RG (también conocido como 'Infra RG')
Do not use deprecated Kubernetes APIs in your YAML manifests	No utilice API de Kubernetes obsoletas en los manifiestos de YAML
Taint Windows nodes	Nodos de Windows taint
Keep windows containers patch level in sync with host patch level	Mantenga sincronizado el nivel de revisión de contenedores de Windows con el nivel de revisión de host
Monitor CPU and memory utilization of the nodes	Supervisar la utilización de la CPU y la memoria de los nodos
If using Azure CNI, monitor % of pod IPs consumed per node	Si usa Azure CNI, supervise % de ip de pod consumidas por nodo
Monitor OS disk queue depth in nodes	Supervisar la profundidad de la cola de disco del sistema operativo en los nodos
If not using egress filtering with AzFW/NVA, monitor standard ALB allocated SNAT ports	Si no utiliza el filtrado de salida con AzFW/NVA, supervise los puertos SNAT alB estándar asignados
Send master logs (aka API logs) to Log Analytics	Enviar registros maestros (también conocidos como registros de API) a Log Analytics
Use the Cluster Autoscaler	Usar el escalador automático de clústeres
Use the Horizontal Pod Autoscaler when required	Utilice el escalador automático de pod horizontal cuando sea necesario
Consider an appropriate node size, not too large or too small	Considere un tamaño de nodo apropiado, no demasiado grande o demasiado pequeño
Configure requests and limits in your pod specs	Configurar solicitudes y límites en las especificaciones del pod
Enforce resource quotas for namespaces	Aplicar cuotas de recursos para espacios de nombres
Ensure your subscription has enough quota to scale out your nodepools	Asegúrese de que su suscripción tenga suficiente cuota para escalar horizontalmente sus nodos
Use automation through ARM/TF to create your Azure resources	Uso de la automatización a través de ARM/TF para crear los recursos de Azure
Use canary or blue/green deployments	Usar implementaciones canarias o azules/verdes
Use KEDA if running event-driven workloads	Usar KEDA si se ejecutan cargas de trabajo controladas por eventos
Use Dapr to ease microservice development	Utilice Dapr para facilitar el desarrollo de microservicios
