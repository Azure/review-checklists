Define non-functional requirements such as SLAs, RTO and RPO.	Defina requisitos no funcionales como SLA, RTO y RPO.
Perform DR tests	Realizar pruebas de DR
Use Traffic Manager or Front Door as a global load balancer for region failover	Utilice Traffic Manager o Front Door como equilibrador de carga global para la conmutación por error de la región
Use Availability Zones if supported in your region	Use zonas de disponibilidad si es compatible con su región
Use the SLA-backed AKS offering	Utilice la oferta AKS respaldada por SLA
Use Disruption Budgets in your pod specs	Utilice presupuestos de interrupción en sus especificaciones de pod
If using a private registry, configure region replication to store images in multiple regions	Si usa un registro privado, configure la replicación de regiones para almacenar imágenes en varias regiones
Use managed identities instead of Service Principals	Usar identidades administradas en lugar de directores de servicio
Integrate authentication with AAD (using the managed integration)	Integrar la autenticación con AAD (mediante la integración administrada)
Limit access to admin kubeconfig (get-credentials --admin)	Limitar el acceso a admin kubeconfig (get-credentials --admin)
Integrate authorization with AAD RBAC	Integrar la autorización con AAD RBAC
Use namespaces for restricting RBAC privilege in Kubernetes	Usar espacios de nombres para restringir el privilegio rbac en Kubernetes
Use Pod identity for workload identities (addon v2 in preview today)	Usar la identidad de Pod para identidades de carga de trabajo (addon v2 en versión preliminar hoy)
Choose the best CNI plugin for your requirements (Azure CNI recommended)	Elija las mejores plugin CNI para sus requisitos (se recomienda Azure CNI)
If using Azure CNI, size your subnet accordingly	Si usa Azure CNI, ajuste el tamaño de la subred en consecuencia
If using Azure CNI, check the maximum pods/node (default 30)	Si usa Azure CNI, compruebe los pods/nodos máximos (valor predeterminado 30)
If using private-IP LoadBalancer services, use a dedicated subnet (not the AKS subnet)	Si utiliza servicios loadbalancer de IP privada, utilice una subred dedicada (no la subred AKS)
Size the service IP address range accordingly (it is going to limit the cluster scalability)	Dimensione el intervalo de direcciones IP del servicio en consecuencia (va a limitar la escalabilidad del clúster)
Filter egress traffic with AzFW/NVA if your security requirements mandate it	Filtre el tráfico de salida con AzFW/NVA si sus requisitos de seguridad lo exigen
If using a public API endpoint, restrict the IP addresses that can access it	Si utiliza una punto de acceso de la API pública, restrinja las direcciones IP que pueden acceder a ella
Use an ingress controller to expose web-based apps	Use un controlador de entrada para exponer aplicaciones basadas en web
If using AGIC, do not share an AppGW across clusters	Si utiliza AGIC, no comparta un AppGW entre clústeres
Use Private Endpoints (preferred) or VNet Service Endpoints to access PaaS from the cluster	Use puntos de conexión privados (preferidos) o puntos de conexión de servicio de red virtual para tener acceso a PaaS desde el clúster
Use private clusters if your requirements mandate it	Utilice clústeres privados si sus requisitos lo exigen
Enable a Kubernetes Network Policy option (Calico/Azure)	Habilitar una opción de directiva de red de Kubernetes (Calico/Azure)
Use Network policies to increase intra-cluster security	Usar directivas de red para aumentar la seguridad dentro del clúster
Use a WAF for web workloads (UIs or APIs)	Usar un WAF para cargas de trabajo web (UI o API)
Use DDoS Standard in the AKS VNet	Utilice DDoS Standard en la red virtual de AKS
Do not use AKS Application Routing Add-On	No utilice AKS Application Routing Add-On
Use the standard ALB (as opposed to the basic one)	Utilice el ALB estándar (en lugar del básico)
If hybrid connectivity is required, use 2xER or ER+VPN for better availability	Si se requiere conectividad híbrida, utilice 2xER o ER+VPN para una mejor disponibilidad
Consider using a service mesh for advanced microservice communication management	Considere la posibilidad de usar un service mesh para la gestión avanzada de la comunicación de microservicios
Store your secrets in Azure Key Vault with the CSI Secrets Store driver	Guarda tus secretos en Azure Key Vault con el controlador de CSI Secrets Store
If user Service Principals for the cluster, refresh credentials periodically (like quarterly)	Si las entidades de servicio de usuario para el clúster, actualice las credenciales periódicamente (como trimestralmente)
Use Azure Policy for Kubernetes to ensure cluster compliance	Use Azure Policy para Kubernetes para garantizar el cumplimiento del clúster
Separate applications from the control plane with user/system nodepools	Separar las aplicaciones del plano de control con las colas de nodo del usuario/sistema
Label your system nodepool to use it with node selectors	Etiquete la cola de nodos del sistema para utilizarla con selectores de nodos
Use a private registry for your images, such as ACR	Utilice un registro privado para sus imágenes, como ACR
Scan your images for vulnerabilities	Escanee sus imágenes en busca de vulnerabilidades
Use Azure Security Center to detect security posture vulnerabiities	Use Azure Security Center para detectar las vulneraciones de la postura de seguridad
Define app separation requirements (namespace/nodepool/cluster)	Definir aplicación requisitos de separación (espacio de nombres/nodepool/cluster)
Use an external application such as kubecost to allocate custs to different users	Utilice una aplicación externa como kubecost para asignar cústides a diferentes usuarios
Use ephemeral OS disks	Utilice discos del sistema operativo efímeros
If not using ephemeral disks, use larger OS disks for the nodes, especially if many pods/node	Si no utiliza discos efímeros, utilice discos del sistema operativo más grandes para los nodos, especialmente si muchos pods/nodo
Avoid keeping state in the cluster, and store data outside (AzStorage, AzSQL, Cosmos, etc)	Evite mantener el estado en el clúster y almacene los datos fuera (AzStorage, AzSQL, Cosmos, etc.)
If using AzFiles Standard, consider AzFiles Premium and/or ANF for performance reasons	Si utiliza AzFiles Standard, considere AzFiles Premium y/o ANF por motivos de rendimiento
If using Azure Disks and AZs, consider dedicating nodes per AZ and WaitForFirstConsumer	Si usa Azure Disks y AZs, considere la posibilidad de dedicar nodos por AZ y WaitForFirstConsumer
Check regularly Azure Advisor for recommendations on your cluster	Consulte regularmente Azure Advisor para obtener recomendaciones en el clúster
Develop your YAML manifests with intelligent text editors such as vscode+kubeadvisor	Desarrolle sus manifiestos YAML con editores de texto inteligentes como vscode+kubeadvisor
Monitor your cluster metrics with Container Insights (or other tools like Prometheus)	Supervise las métricas del clúster con Container Insights (u otras herramientas como Prometheus)
Store and analyze your cluster logs with Container Insights (or other tools like Telegraf/ElasticSearch)	Almacene y analice los registros del clúster con Container Insights (u otras herramientas como Telegraf/ElasticSearch)
Configure Diagnostic Settins in your clusters to send API logs to Azure Monitor	Configure Settins de diagnóstico en los clústeres para enviar registros de API a Azure Monitor
Configure alerts on the most critical metrics (see Container Insights for recommendations)	Configurar alertas en las métricas más críticas (consulte Información sobre contenedores para obtener recomendaciones)
Have a regular process to upgrade the cluster periodically (quarterly, for example)	Tener un proceso regular para actualizar el clúster periódicamente (trimestralmente, por ejemplo)
Use kured for Linux node upgrades	Utilice kured para actualizaciones de nodos Linux
Consider gitops to deploy applications or cluster configuration to multiple clusters	Considere gitops para implementar aplicaciones o configuración de clúster en varios clústeres
Consider spot node pools for non time-sensitive workloads	Considere la posibilidad de agrupaciones de nodos de spot para cargas de trabajo no sensibles al tiempo
Consider AKS virtual node for quick bursting	Considere el nodo virtual AKS para ráfagas rápidas
Make sure no changes are performed by operators in the node RG (aka 'infra RG')	Asegúrese de que los operadores no realicen ningún cambio en el nodo RG (también conocido como 'infra RG')
Use custom Node RG (aka 'Infra RG') name	Utilice el nombre personalizado del nodo RG (también conocido como 'Infra RG')
Do not use deprecated Kubernetes APIs in your YAML manifests	No utilice apis de Kubernetes en desuso en sus manifiestos yaml
Taint Windows nodes	Taint nodos de Windows
Keep windows containers patch level in sync with host patch level	Mantenga el nivel de parche de contenedores de Windows sincronizado con el nivel de parche de host
Monitor CPU and memory utilization of the nodes	Supervisar la utilización de la CPU y la memoria de los nodos
If using Azure CNI, monitor % of pod IPs consumed per node	Si usa Azure CNI, supervise el % de pod direcciones IP consumido por nodo
Monitor OS disk queue depth in nodes	Supervisar la profundidad de la cola de disco del sistema operativo en los nodos
If not using egress filtering with AzFW/NVA, monitor standard ALB allocated SNAT ports	Si no utiliza el filtrado de salida con AzFW/NVA, supervise los puertos SNAT asignados estándar alB
Send master logs (aka API logs) to Log Analytics	Enviar registros maestros (también conocidos como registros de API) a Log Analytics
Use the Cluster Autoscaler	Utilice el escalador automático de clústeres
Use the Horizontal Pod Autoscaler when required	Utilice el autoescalar de pod horizontal cuando sea necesario
Consider an appropriate node size, not too large or too small	Considere un tamaño de nodo adecuado, no demasiado grande o demasiado pequeño
Configure requests and limits in your pod specs	Configure las solicitudes y los límites en las especificaciones del pod
Enforce resource quotas for namespaces	Aplicar cuotas de recursos para espacios de nombres
Ensure your subscription has enough quota to scale out your nodepools	Asegúrese de que su suscripción tenga suficiente cuota para escalar horizontalmente las colas de nodos
Use automation through ARM/TF to create your Azure resources	Use la automatización a través de ARM/TF para crear los recursos de Azure
Use canary or blue/green deployments	Usar implementaciones canarias o azules/verdes
Use KEDA if running event-driven workloads	Use KEDA si ejecuta cargas de trabajo controladas por eventos
Use Dapr to ease microservice development	Utilice Dapr para facilitar el desarrollo de microservicios
