description: 'Upgrading the API Management instance to the Premium SKU adds support
  for Availability Zones, enhancing availability and resilience by distributing services
  across physically separate locations within Azure regions.

  '
guid: baf3bfc0-32a2-4c0c-926d-c9bf0b49808e
labels:
  area: High Availability
  source: azure-resources/ApiManagement/service/recommendations.yaml
  sourceType: aprl
links: []
queries:
- arg: '// Azure Resource Graph Query

    // Find all API Management instances that aren''t Premium

    resources

    | where type =~ ''Microsoft.ApiManagement/service''

    | extend skuName = sku.name

    | where tolower(skuName) != tolower(''premium'')

    | project recommendationId = "baf3bfc0-32a2-4c0c-926d-c9bf0b49808e", name, id,
    tags, param1=strcat("SKU: ", skuName)


    '
resourceTypes:
- Microsoft.ApiManagement/service
service: APIM
severity: 0
text: Migrate API Management services to Premium SKU to support Availability Zones
